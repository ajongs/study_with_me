<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CommentMapper" >
    <insert id="insertComment" parameterType="domain.Comment">
        INSERT INTO
            testdb.comment(comment_seq,
                           board_seq,
                           order_num,
                           comment_content,
                           comment_writer,
                           comment_id,
                           comment_date)
        VALUES
            (
                null,
                #{board_seq},
                #{order_num},
                #{comment_content},
                #{comment_writer},
                #{comment_id},
                now()
            );
        UPDATE testdb.comment SET group_num = (SELECT last_insert_id()) WHERE comment_seq = (SELECT last_insert_id());
    </insert>
    <insert id="insertReply" parameterType="domain.Comment">
        INSERT INTO
            testdb.comment
        VALUES
            (
                null,
                #{board_seq},
                #{group_num},
                #{order_num},
                #{comment_content},
                #{comment_writer},
                #{comment_id},
                now()
            )
    </insert>
    <select id="getComment" parameterType="int" resultType="domain.Comment">
        SELECT * FROM testdb.comment WHERE board_seq=#{seq} ORDER BY group_num, order_num
    </select>
</mapper>
<!--UPDATE testdb.comment SET group_num = (SELECT last_insert_id()) WHERE comment_seq = (SELECT last_insert_id());-->
<!-- (SELECT IFNULL(MAX(comment_seq)+1, 1) FROM testdb.comment c), -->